name: ci

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build-and-test:
    permissions:
      contents: read
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Build and test mock path
        run: make test-mock
